<%= form_for(@student_area_type) do |f| %>
  <% if @student_area_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_area_type.errors.count, "error") %> prohibited this student_area_type from being saved:</h2>

      <ul>
      <% @student_area_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :area_type_name %><br />
    <%= f.text_field :area_type_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
