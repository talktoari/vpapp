<h1>Listing donation_types</h1>

<table>
  <tr>
    <th>Donation type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @donation_types.each do |donation_type| %>
  <tr>
    <td><%= donation_type.donation_type %></td>
    <td><%= link_to 'Show', donation_type %></td>
    <td><%= link_to 'Edit', edit_donation_type_path(donation_type) %></td>
    <td><%= link_to 'Destroy', donation_type, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Donation type', new_donation_type_path %>
