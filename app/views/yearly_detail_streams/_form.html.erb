<%= form_for(@yearly_detail_stream) do |f| %>
  <% if @yearly_detail_stream.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@yearly_detail_stream.errors.count, "error") %> prohibited this yearly_detail_stream from being saved:</h2>

      <ul>
      <% @yearly_detail_stream.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :stream %><br />
    <%= f.text_field :stream %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
