<fieldset>
<legend>Upload Donor</legend>
	<h3> Choose Upload file</h3>
	<h4>Uploads, Parses, Validates and stores the Donors Data in DB</h4>
	<%= form_tag('/donors/upload_validate_donor', :multipart => true) %>
	<p>
		<%= file_field_tag 'excel_file' %>
	</p>
	<p>
		<%= submit_tag "Start Upload" %>
	</p>
	</form>
</fieldset>

