<h2>New User Sign Up Here</h2>
<h3>You can contact VidyaPoshak Admins to register a new account.</h3>
<h3>Please go back to Home Page by Clicking above</h3>

<!--
<fieldset>
<legend>Fill this Sign Up Form</legend>
<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

  <div><%= f.label :email %>
  <%= f.email_field :email %></div>

  <div><%= f.label :password %>
  <%= f.password_field :password %></div>

  <div><%= f.label "Confirm Pwd" %>
  <%= f.password_field :password_confirmation %></div>

  <div><%= f.label :role %>
  <% if (user_signed_in?) && (current_user.role == "super_admin") %>
    <%= f.collection_select :role, User::ROLESADM, :to_s, :humanize %></div>
  <% else %>
    <%= f.collection_select :role, User::ROLESADM, :to_s, :humanize %></div>
  <% end %>

  <br />
  <div><%= f.submit "Sign up" %></div>
<% end %>
</fieldset>

<%#<%= render :partial => "devise/shared/links" %>
-->

