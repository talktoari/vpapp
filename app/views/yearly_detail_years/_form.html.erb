<%= form_for(@yearly_detail_year) do |f| %>
  <% if @yearly_detail_year.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@yearly_detail_year.errors.count, "error") %> prohibited this yearly_detail_year from being saved:</h2>

      <ul>
      <% @yearly_detail_year.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :year %><br />
    <%= f.number_field :year %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
